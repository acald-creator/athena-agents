# Grimoire workbench UI — host SvelteKit on :4400 (proxies /api to cargo :3010)
# Schema matches other athena-agents target files.

[target]
id = "grimoire"
host = "127.0.0.1"
port = 4400
protocol = "http"
base_path = "/"
timeout_seconds = 5
